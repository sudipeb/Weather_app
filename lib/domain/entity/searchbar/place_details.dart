class Place {
  final String name;
  final double latitude;
  final double longitude;

  /// Creates a new [Place] instance with the given [name], [latitude], and [longitude].
  Place({required this.name, required this.latitude, required this.longitude});
}
